(ns ovo.core
  (:gen-class)
  (:require [clojure.math.numeric-tower :refer [expt]]
           [schema.core :as s]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
